module.exports = function(Project) {
  /*
  Project.afterRemote('find', function(ctx, user, next){

    if (ctx.accessToken)
      console.log(ctx.accessToken.userId);
    var res = [];
    if(ctx.result) {
      if(Array.isArray(ctx.result)) {
        ctx.result.forEach(function (result) {
          if (result.companyid === 1)
            res.push(result);
        });
      } else {

      }
    }
    ctx.result = res;
    next();
  });
*/
};
